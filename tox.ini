[tox]
envlist = py36, py37

[testenv]
deps = pipenv
commands =
    pipenv install --dev
    pipenv run pytest tests

[testenv:lint]
commands=
    pipenv install --dev
    pipenv run flake8 --version
    pipenv run flake8 sample tests
